[project]
name = "edgelog"
version = "0.1.0"
description = "A trade journal that tells you if you actually have an edge."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "python-multipart>=0.0.9",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "httpx>=0.27",
]
